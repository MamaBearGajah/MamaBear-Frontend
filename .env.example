# API Backend (set when backend is confirmed)
NEXT_PUBLIC_API_URL=http://localhost:3000/api

# App
NEXT_PUBLIC_APP_NAME=Mamabear
NEXT_PUBLIC_APP_URL=http://localhost:3001

# Development only — mock admin session (skip login)
# MOCK_ADMIN_AUTH=true

# Development only — mock product list (12 sample products)
MOCK_PRODUCTS=true
NEXT_PUBLIC_MOCK_PRODUCTS=true
