import CategoryFilter from "./CategoryFilter"
import CostFilter from "./CostFilter"


export default async function FilterSection({ productId }: { productId: number }) {
    return(
        <div>
            Filter Section
        </div>
    )
}